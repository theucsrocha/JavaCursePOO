package application;

import java.util.Date;
import java.util.Scanner;

import agregation_exercise.WorkerLevel;
import entites.HourContract;
import entites.Worker;

public class main {

	public static void main(String[] args) {

		Scanner sc = new Scanner(System.in);

		System.out.println("Enter the departament:");
		String departament = sc.nextLine();

		System.out.println("Enter woeker data:");

		System.out.println("Name:");
		String name = sc.nextLine();

		System.out.println("Level:");
		sc.nextLine();
		String level = sc.nextLine();

		System.out.println("Base salary:");
		Double salary = sc.nextDouble();

		Worker worker = new Worker(name, WorkerLevel.valueOf(level), salary);

		System.out.println("How many contracts to this worker?");
		int n = sc.nextInt();

		for (int i = 0; i < n; i++) {
			Date date = null;
			sc.nextLine();
			System.out.println("Date:");
			String sDate = sc.nextLine();
			date.parse(sDate);

			System.out.println("Value per hour:");
			Double valueHour = sc.nextDouble();

			System.out.println("Duration:");
			int duration = sc.nextInt();
			HourContract c = new HourContract(date, valueHour, duration);
			worker.addContract(c);

		}
		
		System.out.println("Enter the month and year to calculate income");
		
		String sDate = sc.nextLine();
		Date date = null;
		date.parse(sDate);
		
		System.out.printf("Name:" + worker.getName() + "%n" + "Departament:" + departament + "%n" + "Income for" + date + worker.income(date.getYear(),date.getMonth()));
		
		sc.close();
	}
	

}
